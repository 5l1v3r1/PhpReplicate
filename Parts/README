PARTS File:
-----------

Everything is first tested before it is put together. 
The first few parts are:

1. Detect the OS of the server OSDetect.php

2. Check if system command is enabled. isinfo.php

3. Check if exec command is enabled. excinfo.php


4. Replicates itself.   mv.php ( Linux Routine )

5. Replicates itself.  cp.php (Windows Server Routine) Probably coded in powershell.

6. Payload part

7. Part that chooses what to do based on OS detected.

8. Entropy generator.

9. Obsufucation part
